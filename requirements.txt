fastapi==0.136.1  # 釘版（見下方 starlette）；亦避開 0.136.3 typosquat MAL-2026-4750
starlette==1.1.0  # 釘版：starlette 1.3.x 起 include_router 產生 _IncludedRouter（無 .path），破壞全部路由內省測試；上限放開時 CI 會漂到 1.3.1。後續宜改用 lockfile 統一釘版
uvicorn>=0.24.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0  # 讀取 .xls (Excel 97-2003) 用於匯入舊版考核 / 年終經營績效表
python-multipart>=0.0.27  # CVE-2026-40347, CVE-2026-42561（修補版 0.0.27；專案 requires-python>=3.10）
reportlab>=4.0.0
Pillow>=12.2.0  # CVE-2026-25990, CVE-2026-40192, CVE-2026-42308/9/10/11, PYSEC-2026-165（修補版 12.2.0；專案 requires-python>=3.10）
# pillow-heif 可選：若部署環境有 libheif 且安裝成功，HEIC 上傳會自動轉 JPG；
# 未安裝則拒絕 HEIC 上傳並提示使用者改用 JPG/PNG。
pillow-heif>=0.16.0
pydantic>=2.0.0
pydantic-settings>=2.0,<3.0
holidays>=0.30.0
python-jose>=3.3.0
python-dateutil>=2.8.0
cachetools>=5.3.0
python-dotenv>=1.2.2  # CVE-2026-28684（修補版 1.2.2；專案 requires-python>=3.10）
alembic>=1.13.0
requests>=2.33.0  # CVE-2026-25645（修補版 2.33.0；專案 requires-python>=3.10）
supabase>=2.0.0  # 物件儲存（activity_posters / leave_attachments）
python-json-logger>=2.0.0
pytest==9.0.3  # CVE-2025-71176；釘版對齊本地驗證環境（CI 漂到 9.1.1 未在本地驗證）
pytest-cov>=4.1.0
pytest-split>=0.11.0  # CI 分片：matrix 多 job 各跑 1/N 子集（單進程序列，避 xdist 並發 flaky）
httpx>=0.24.0
responses>=0.25
mcp>=1.0.0  # MCP server SDK（mcp_server/activity_crud 暴露才藝 CRUD 給 Claude Code）
sentry-sdk[fastapi]>=2.0.0  # 錯誤監控；缺 SENTRY_DSN 時 init_sentry() no-op
redis>=5.0,<6  # async client（redis.asyncio），broadcast + 未來 cache 共用
fakeredis>=2.20,<3  # test only；fakeredis.aioredis 仿真 Redis async + pubsub
ruff>=0.5  # linting（DTZ rule + per-file-ignores）
