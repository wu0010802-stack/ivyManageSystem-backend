# Environment
.env
.env.local
.env.*.local

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.coverage
htmlcov/
*.egg-info/
dist/
build/

# Virtual environments
.venv
env/
venv/
venv_sec/
ENV/

# Database
*.db
*.sqlite3
data/payroll.db

# Logs
*.log
backend.log

# OpenAPI dump（scripts/dump_openapi.py 產出）：含全 30+ router schema，
# 是給前端 codegen 用的本地產物，commit 進 repo 等於對外洩漏完整 admin API surface
openapi.json

# Uploads (user data)
# 所有使用者上傳檔案集中在 data/uploads/<module>/
# 舊 uploads/ 已棄用，保留 ignore 規則避免誤 commit 歷史遺留目錄
uploads/
data/uploads/
data/activity_posters/

# Offboarding certificates（generated PDFs，不入 repo）
storage/offboarding_certificates/

# IDE
.vscode/
.idea/
*.swp
.DS_Store

# Migration scripts (one-time use)
add_columns.py
add_indexes.py
add_job_title_fk.py
import_roster.py
migrate_*.py
# DR tooling: idempotent, tested, permanent — must NOT be excluded
!scripts/migrate_growth_reports_to_supabase.py
reset_employees.py
swap_employee_columns.py
update_*.py
setup_db.py
query_debug.py
init_holidays.py
.worktrees/
